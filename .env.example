# Copy to .env.local for local overrides (gitignored via the *.local rule),
# or set as a Netlify site environment variable for the deployed build.
# Unset -> defaults to the demo domain (see src/App.jsx).

# VITE_PARTICIPANT_BASE_URL=https://demoinqua.cotrav.co.in
# VITE_PARTICIPANT_BASE_URL=https://inqua.cotrav.co.in
